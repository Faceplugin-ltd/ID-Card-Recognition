Import `DocumentReader-API.postman_collection.json`.

baseUrl: `http://127.0.0.1:8082`

Flow: Health → Version (`/api/backend`) → Machine code → License status → Activate (FP1.) → Document Process (`Authenticity: normal`)
